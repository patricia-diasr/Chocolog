import React from "react";
import { Box, Text, Center } from "native-base";

export const FlavorListScreen = () => (
    <Center flex={1}>
        <Text fontSize="xl">flavor-list</Text>
    </Center>
);
